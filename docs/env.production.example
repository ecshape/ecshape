# PRODUCTION ENVIRONMENT CONFIGURATION - OPTIMIZED FOR MINIMAL RESOURCES
# Copy this file to .env.production and update with your actual values

# Environment
ENVIRONMENT=production

# Domain Configuration
DOMAIN=your-actual-domain.com
CORS_ORIGINS=https://your-actual-domain.com,https://www.your-actual-domain.com

# Security
# Generate a secure JWT secret: openssl rand -hex 32
JWT_SECRET=your-new-secure-jwt-secret-here-32-chars-minimum
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Database Configuration - OPTIMIZED FOR MINIMAL RESOURCES
# SQLite (recommended for small scale - 20 users max)
DATABASE_URL=sqlite:///./data/elior_fitness.db

# File Upload Configuration - REDUCED FOR MINIMAL STORAGE
MAX_FILE_SIZE=5242880  # 5MB (reduced from 10MB)
UPLOAD_DIR=/app/uploads

# Performance Configuration - OPTIMIZED FOR MINIMAL RESOURCES
WORKERS=1  # Single worker for minimal memory usage
LOG_LEVEL=WARNING  # Reduced logging for performance
ENABLE_DEBUG_LOGGING=false

# Resource Limits
MAX_CONCURRENT_REQUESTS=50  # Limit concurrent requests
MAX_REQUESTS_PER_WORKER=1000  # Restart worker after 1000 requests 